<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.lol.wazirbuild.msilib">

    <application
        android:allowBackup="true"
        android:icon="@drawable/msit_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".notes_category_Activity"></activity>
        <activity android:name=".notes_Activity" /> <!-- tools:node="replace"> -->
        <activity android:name=".RegistrationScreen" />
        <activity android:name=".mainScreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>